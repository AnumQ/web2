<div id="item">
	<div id="image">
	<p>
	  <%= image_tag(@product.image_url, :size => "400x400") %>
	</p>
	</div>
	
	<div id="title">
	<p>
	  <b> Title:
	  <%= @product.title %>
	  </b>
	</p>
	
	<p>
	  <b>Description:</b>
	  <%= @product.description %>
	</p>
	
	<p>
	  <b>Price:</b>
	  <b><%= sprintf("£%0.02f", @product.price) %> </b>
	</p>
	

	<%=button_to 'Add to Cart',line_items_path(:product_id => @product),  :class => 'button' %>

	</br> 
	<% if user_signed_in? %>
	  <% if  current_user.admin? %>
	   <%= link_to 'Edit', edit_product_path(@product) %> |
	   <%= link_to 'Back', products_path %>
	  <% else %>
	  <%= link_to 'Back', store_path %>
	  <% end %>
	  <% else %> 
	  <%= link_to 'Back', store_path %>
	<% end %>
   </div>
</div>