<% @products.each do |product| %>
	<div class="product">
		<div class="im2"><%= image_tag(product.image_url, :class=> 'im', :size => "100x150") %></div>
		<div class="desc">
			<p class="name"><%= link_to  product.title, product %></p>

			<p><%= sanitize(product.description) %></p>
			<p>Product code: <span>1456</span></p>
		</div>
		<div class="price-box">
			<% d = sprintf("£%0.02f",product.price) %>
			<p><span class="price"><%= d[0,3] %><sup><%= d[3,7] %></sup></span></p>
		</div>
	<div id="add2Cart">
	</div>
		<%=link_to "Add", line_items_path(:product_id => product) %>
	
	</div>

<% end %>