<% @products.each do |product| %>
<div id="product-wrapper">
	<div class="product">
		<div class="im2"><%= link_to image_tag(product.image_url, :class=> 'im', :size => "100x100"), product %></div>
		<div class="desc">
			<p class="name"><%= link_to product.title, product %></p>
			<p><%=truncate(strip_tags(product.description),:length=>40)%></p>
			<p>Product code: <span>1456</span></p>
		</div>
		<div class="price-box">
			<% d = sprintf("£%0.02f",product.price) %>
			<p><span class="price"><%= d[0,3] %><sup><%= d[3,7] %></sup></span></p>
		</div>
			<div id="add2Cart">
			<%= button_to "Add to Cart", line_items_path(:product_id => product), :class => 'button' %>
	</div>
	</div>

 </div>
<% end %>